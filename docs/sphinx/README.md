# Sphinx entry point

The Sphinx configuration is `docs/conf.py` so the English and Japanese Markdown trees can be referenced without duplication.

```bash
sphinx-build -b html docs docs/_build/sphinx
```
